{{#isBasicBearer}}import requests

resp = requests.get(
  "https://api.kairos.example/",
  headers={"Authorization": "Bearer YOUR_ACCESS_TOKEN"},
){{/isBasicBearer}}{{#isBasicBasic}}import requests

resp = requests.get(
  "https://api.kairos.example/",
  headers={"Authorization": "Basic BASE64(username:password)"},
){{/isBasicBasic}}{{#isApiKey}}import requests

resp = requests.get(
  "https://api.kairos.example/",
  headers={"{{keyParamName}}": "ApiKey YOUR_API_KEY"},
){{/isApiKey}}
